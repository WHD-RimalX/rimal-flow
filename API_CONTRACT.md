# عقد الواجهات البرمجية (API Contract) — رمال فلو v1

هذا المستند يوثّق كل نقاط النهاية (Endpoints) الفعلية في مشروع رمال فلو (Rimal Flow)، مع التركيز على
مسارات **الحجوزات**، **المسح بالباركود (QR Check-in/Check-out)**، و**التسكين اليدوي/البحث عن العملاء**،
تحضيراً للتكامل مع أي عميل خارجي (تطبيق جوال، لوحة شريك، إلخ) وللنشر على GitHub/Vercel.

> **تحديث**: تم تنفيذ الترحيل الموصوف في **§10 مطابقة معايير التكامل** بالكامل — مسارات الحجوزات
> والمصادقة أدناه تعكس الآن `INTEGRATION_STANDARDS.pdf` الرسمي (مجموعة اختبارات Postman المرسلة من
> الإدارة هي المرجع النهائي). راجع §10 لسجل القرار وتفاصيل كل نقطة تحوّل.

---

## 1. عام

| | |
|---|---|
| **Base URL (محلي)** | `http://localhost:3000` |
| **Base URL (إنتاج)** | `https://<your-vercel-domain>.vercel.app` |
| **الصيغة** | `application/json` لكل الطلبات والاستجابات |
| **المصادقة** | NextAuth.js — جلسة JWT محفوظة في كوكي `next-auth.session-token` (HttpOnly). لا يوجد Bearer token منفصل. |
| **الأدوار (Roles)** | `USER` (عميل) · `RECEPTION` (استقبال) · `ADMIN` (مدير) · `SUPER_ADMIN` (مدير عام) |
| **الحجم القصى للـ IDs** | `cuid()` — نصوص فريدة، وليست أرقاماً تسلسلية |

### صيغة الخطأ الموحّدة

كل خطأ يُعاد بنفس الشكل:

```json
{
  "error": "نص الخطأ بالعربية للعرض المباشر للمستخدم",
  "issues": {
    "formErrors": [],
    "fieldErrors": { "phone": ["رقم جوال غير صالح — مثال صحيح: 0512345678 أو +966512345678"] }
  }
}
```
- `issues` يظهر فقط في أخطاء التحقق من المدخلات (Zod validation، حالة 400).
- رموز الحالة المستخدمة: `400` بيانات غير صالحة · `401` **لا توجد جلسة إطلاقاً** · `403` **جلسة صالحة
  لكن الدور/الصلاحية غير كافية** (401 و403 لا يُخلَطان أبداً — مطابقة صريحة لمتطلبات الاختبار الأمني) ·
  `404` غير موجود · `409` تعارض/تكرار · `422` مرفوض منطقياً (Business rule) · `429` طلبات متكررة
  (Anti-fraud cooldown) · `500` خطأ سيرفر غير متوقع.

---

## 2. المصادقة (Auth)

### `POST /api/auth/register`
تسجيل عميل جديد (دور `USER` دائماً).

**Request body**
```json
{
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "phone": "0512345678",
  "password": "Passw0rd",
  "isStudent": false,
  "studentIdNumber": "441xxxxxx"
}
```
**Response `201`**
```json
{ "user": { "id": "cl...", "name": "أحمد محمد", "email": "ahmed@example.com", "phone": "0512345678", "isStudent": false } }
```
**أخطاء**: `400` بيانات غير صالحة (مع `issues.fieldErrors` لكل حقل) · `409` البريد/الجوال مستخدم مسبقاً.

### `POST /api/auth/callback/credentials` (عبر NextAuth)
تسجيل الدخول. يُستخدم من الواجهة عبر `signIn("credentials", { email, password })` — لا يُستدعى مباشرة من عميل خارجي عادة. يُعيد كوكي جلسة عند النجاح.

### `GET /api/auth/session`
يُعيد الجلسة الحالية (أو `{}` إن لم توجد) — تُستخدم داخلياً من next-auth.

---

## 3. المساحات (Spaces)

### `GET /api/spaces`
عام (لا يتطلب تسجيل دخول). يُعيد كل المساحات النشطة وبنية أسعارها.

**Response `200`**
```json
{
  "spaces": [
    {
      "id": "cl...", "slug": "shared-workspace", "name": "مساحة عمل مشتركة",
      "capacityUnits": 17,
      "hourlyPrice": "20", "fourHourPrice": "60", "dailyPrice": "115",
      "monthlyMorningPrice": "999", "monthlyEveningPrice": "1099",
      "studentDiscount": "0.15", "isActive": true
    }
  ]
}
```
> حقول الأسعار والخصم من نوع `Decimal` في قاعدة البيانات وتُسلسَل كنصوص (`string`) في JSON.

---

## 4. البحث عن العملاء (Customer Search — للموظفين فقط)

### `GET /api/users/search?q=<نص>`
**Auth**: يتطلب جلسة موظف (`RECEPTION`/`ADMIN`/`SUPER_ADMIN`) — `403` لغير الموظفين.

يبحث في الاسم أو رقم الجوال فقط (غير حساس لحالة الأحرف، ويتجاهل المسافات/الشرطات داخل رقم الجوال).
لا يبحث بالبريد الإلكتروني. أقل طول للاستعلام: حرفان.

**Response `200`**
```json
{
  "users": [
    { "id": "cl...", "name": "سارة العتيبي", "phone": "0559876543", "email": "s@x.com", "isStudent": true, "role": "USER" }
  ]
}
```
النتائج محدودة بـ 8 عناصر، مرتّبة أبجدياً بالاسم.

---

## 5. الحجوزات (Bookings)

### `POST /api/bookings`
إنشاء حجز. **الأسعار تُحسب بالكامل على السيرفر** — لا يوجد أي حقل سعر في جسم الطلب.
**Auth**: يتطلب جلسة دائماً — `401` لأي طلب بلا جلسة، حتى لو كان يحمل بيانات ضيف كاملة. لا يوجد
"حجز ضيف مجهول تماماً" بلا أي جلسة إطلاقاً؛ الموظف يقدر يحجز لصالح ضيف عبر `guestName`+`guestPhone`
لكن دائماً تحت جلسته هو.

**قاعدة إلزامية للموظفين**: إذا كان المستدعي موظفاً (`RECEPTION`/`ADMIN`/`SUPER_ADMIN`)، يجب تحديد
المستفيد الفعلي من الحجز صراحة — إما `customerUserId` (عميل مسجَّل) أو `guestName`+`guestPhone`
(ضيف). حجز "مجرّد" منسوب لحساب الموظف نفسه بلا مستفيد محدَّد **مرفوض** (`400`).

**Request body**
```json
{
  "spaceId": "cl...",
  "bookingType": "hourly",
  "startDate": "2026-07-22T10:00:00.000Z",
  "endDate": "2026-07-22T11:00:00.000Z",
  "guests": 2,
  "isStudent": false,
  "studentIdNumber": "441xxxxxx",
  "notes": "ملاحظة اختيارية",

  "customerUserId": "cl...",

  "guestName": "خالد",
  "guestPhone": "0512345678",
  "guestEmail": "khaled@example.com"
}
```
| الحقل | نوع | ملاحظات |
|---|---|---|
| `spaceId` | string (مطلوب) | |
| `bookingType` | `hourly \| four_hour \| daily \| monthly_morning \| monthly_evening` (مطلوب) | **غير حساس لحالة الأحرف** — يُطبَّع داخلياً لحروف كبيرة. يجب أن تدعمه المساحة (سعر غير null) وإلا `422`. |
| `startDate` | ISO datetime (مطلوب) | لا يقبل وقتاً ماضياً. يقبل أيضاً `startTime` كاسم مرادف قديم لنفس الحقل. |
| `endDate` | ISO datetime (اختياري) | يُتحقَّق فقط من أنه بعد `startDate` (وإلا `400`) — **لا يُستخدم لحساب مدة الحجز الفعلية**؛ تلك تُحسب سيرفرياً من `bookingType` حصراً حتى لو أُرسِل نطاق مختلف (دفاع في العمق ضد التلاعب بالسعر/المدة). |
| `guests` | number (اختياري، 1–50) | عدد الضيوف — بوابة تحقق أولية بحد أقصى ثابت (`400` إن تُجووِز). |
| `isStudent` | boolean | يُتجاهل إن اختير `customerUserId` — يُعتمد بدلاً منه `user.isStudent` الفعلي من قاعدة البيانات |
| `customerUserId` | string (اختياري) | عميل مسجَّل — من نتائج `/api/users/search` |
| `guestName`/`guestPhone`/`guestEmail` | (اختياري) | لحجز الضيف (يتطلب جلسة موظف كما سبق)؛ `guestPhone` بصيغة `05XXXXXXXX` أو `+9665XXXXXXXX` |

**Response `201`** — كائن الحجز مباشرةً (بدون تغليف)، الحالة الابتدائية دائماً `PENDING`:
```json
{
  "id": "cl...", "bookingCode": "RMX-7F3K2A9Q",
  "spaceId": "cl...", "bookingType": "HOURLY",
  "startTime": "2026-07-22T10:00:00.000Z", "endTime": "2026-07-22T11:00:00.000Z",
  "startDate": "2026-07-22T10:00:00.000Z", "endDate": "2026-07-22T11:00:00.000Z",
  "isStudent": false, "basePrice": "20", "discountAmount": "0", "finalPrice": "20",
  "status": "PENDING", "space": { "...": "..." }
}
```
> `startDate`/`endDate` مرادفان لـ `startTime`/`endTime` بنفس القيمة بالضبط (نفس اللحظة الزمنية) —
> الحقلان القديمان يبقيان لتوافق لوحة التحكم الداخلية، والجديدان للامتثال لعقد التكامل الرسمي.
> أي حقل يحاول العميل حقنه (`status`, `userId`, `id`, `paymentStatus`, `totalPrice`...) يُتجاهل تماماً
> أو يُرفض — لا يوجد في `createBookingSchema` أصلاً، فلا يصل لقاعدة البيانات إطلاقاً.

**أخطاء شائعة**: `401` لا جلسة · `400` بيانات غير صالحة/مستفيد غير محدَّد/عدد ضيوف غير معقول ·
`404` المساحة أو العميل المحدَّد غير موجود ·
`409` تعارض حجز (تجاوز الطاقة الاستيعابية للمساحة في نفس التوقيت) · `422` باقة السعر غير متاحة لهذه المساحة.

### `GET /api/bookings`
**Auth**: يتطلب جلسة (`401` بدونها). الموظفون يرون كل الحجوزات؛ العميل العادي يرى حجوزاته فقط.

**Query params** (كلها اختيارية):

| Param | مثال | الوصف |
|---|---|---|
| `date` | `2026-07-22` | يوم كامل (00:00 → 23:59:59) |
| `from`, `to` | ISO datetime | نطاق زمني حر (يُستخدم في التقويم) |
| `status` | `CHECKED_IN` أو `CHECKED_IN,CHECKED_OUT` | قيمة واحدة أو عدة قيم مفصولة بفاصلة |
| `spaceId` | `cl...` | تصفية بمساحة واحدة |
| `page`, `pageSize` | `1`, `10` | **اختياريان**: عند تمرير أيّ منهما تُفعَّل الصفحات (الحد الأقصى `pageSize=50`)؛ بدونهما تُعاد كل النتائج المطابقة دفعة واحدة (للتوافق مع التقويم/الخريطة) |

**Response `200`**
```json
{
  "bookings": [ { "...": "كائن حجز كامل مع space وuser وcheckInLogs (آخر 5 سجلات) وstartDate/endDate" } ],
  "total": 42,
  "page": 1,
  "pageSize": 10
}
```

### `GET /api/bookings/:id`
**Auth**: صاحب الحجز أو موظف (`401` بلا جلسة، `403` لغير المخوَّلين رغم وجود جلسة صالحة، `404` إن لم يوجد).
الاستجابة كائن الحجز مباشرةً (بدون تغليف)، بنفس شكل استجابة الإنشاء.

### `PATCH /api/bookings/:id`
**تخصيص المقعد المرئي فقط** (`seatIndex`) على خريطة المقر — لموظفي رمال فلو حصراً. لم يعد هذا المسار
يقبل تغيير `status` إطلاقاً (حتى لموظف) — أي جسم يحتوي مفتاح `status` يُرفض بـ`403` صراحةً؛ تحويلات
الحالة انتقلت بالكامل إلى `PATCH /api/admin/bookings/:id` أدناه. محاولة تغيير حالة الحجز من هنا (حتى
من مالك الحجز نفسه) محظورة بنيوياً — هذا هو حاجز "الموافقة الذاتية" (Self-Approval) الأمني.

**Request body**
```json
{ "seatIndex": 4 }
```
`seatIndex: null` يزيل التخصيص. **Auth**: موظفون فقط — `401` بلا جلسة، `403` لجلسة غير موظف.

### `DELETE /api/bookings/:id`
**إلغاء ذاتي** — صاحب الحجز يلغي حجزه الخاص فقط (لا يتطلب صلاحية `canCancelBooking` الإدارية).
`401` بلا جلسة · `403` إن لم يكن المستدعي هو صاحب الحجز (بصرف النظر عن دوره — لا كشف IDOR) ·
`404` إن لم يوجد الحجز · `409` إن كان ملغى بالفعل. الاستجابة عند النجاح: كائن الحجز بعد التحديث
(`status: "CANCELLED"`)، مباشرةً بدون تغليف.

---

## 5.ب المسارات الإدارية (Admin — لموظفي رمال فلو فقط)

### `PATCH /api/admin/bookings/:id`
تحويل حالة الحجز (دورة الحياة: `PENDING → CONFIRMED → CHECKED_IN → CHECKED_OUT / CANCELLED / NO_SHOW`)
و/أو تخصيص مقعده. **Auth**: موظفون فقط (`401` بلا جلسة، `403` لجلسة USER عادية — **وليس 401**، لأن
الجلسة صالحة والمشكلة صلاحية الدور فقط). الإلغاء (`CANCELLED`) يتطلب صلاحية `canCancelBooking`
تحديداً (ADMIN/SUPER_ADMIN فقط، وليست RECEPTION).

**Request body**
```json
{ "status": "CANCELLED", "reason": "طلب العميل" }
```
الاستجابة عند النجاح: كائن الحجز بعد التحديث مباشرةً (بدون تغليف).

### `DELETE /api/admin/bookings/:id`
إلغاء أي حجز بصرف النظر عن مالكه. **Auth**: يتطلب صلاحية `canCancelBooking` (ADMIN/SUPER_ADMIN فقط)
بجانب كون المستدعي موظفاً أصلاً. `403` لأي مستخدم آخر (بما فيهم RECEPTION) رغم وجود جلسة صالحة.

---

## 5.ج توفر الأوقات (Availability)

### `GET /api/availability?spaceId=cl...&date=2026-07-22`
عام (لا يتطلب تسجيل دخول، مثل `/api/spaces`). يُعيد فتحات بالساعة ضمن ساعات دوام المساحة في اليوم
المحدَّد (`weeklyAvailability`)، كل فتحة مع `isAvailable` بناءً على تعارضها الفعلي مع حجوزات نشطة
مقابل `capacityUnits` للمساحة.

**Response `200`**
```json
{
  "slots": [
    { "startTime": "2026-07-22T08:00:00.000Z", "endTime": "2026-07-22T09:00:00.000Z", "isAvailable": true },
    { "startTime": "2026-07-22T09:00:00.000Z", "endTime": "2026-07-22T10:00:00.000Z", "isAvailable": false }
  ]
}
```
**أخطاء**: `400` نقص `spaceId`/`date` أو تاريخ غير صالح · `404` المساحة غير موجودة/غير نشطة.

---

## 6. تسجيل الحضور/الانصراف عبر QR (Check-in Engine)

### `POST /api/checkin`
**Auth**: لا يتطلب جلسة إلزامياً (يعمل ذاتياً عبر مسح العميل نفسه)، لكن إن وُجدت جلسة موظف تُسجَّل كمنفِّذ الإجراء.

**Request body**
```json
{ "bookingCode": "RMX-7F3K2A9Q", "action": "CHECK_IN", "qrCode": "RIMALX-HQ-MAIN-BRANCH-0001" }
```
| الحقل | ملاحظات |
|---|---|
| `bookingCode` | كود الحجز الظاهر للعميل |
| `action` | `CHECK_IN` أو `CHECK_OUT` |
| `qrCode` | يجب أن يطابق قيمة `NEXT_PUBLIC_VENUE_QR_CODE` الثابتة للمقر — وإلا `400` |

**القيود المطبَّقة (مرتّبة حسب الفحص):**
1. رمز QR غير مطابق → `400`.
2. الحجز ملغى/عدم حضور → `422`.
3. تكرار نفس الإجراء (Check-in أو Check-out) خلال أقل من **3 دقائق** من آخر محاولة → `429` (مكافحة احتيال) —
   تُقارَن فقط بآخر سجل فعلي بصرف النظر عن نوعه؛ إن كان آخر سجل هو الإجراء المعاكس (مثال: انصرف ثم
   يريد الدخول مجدداً) لا يوجد أي انتظار.
4. **Check-in**: مرفوض إن كانت الحالة `CHECKED_IN` بالفعل (`409`). عودة بعد `CHECKED_OUT` سابق **مسموحة**
   (الحجز لا "ينتهي" بمجرد الانصراف — الرصيد المتبقي يُستأنف من حيث توقّف). نافذة **±30 دقيقة** حول
   موعد الحجز تُطبَّق فقط على *أول* تسجيل دخول للحجز؛ العودات اللاحقة تُقيَّد فقط بانتهاء `endTime`
   الإجمالي ووجود رصيد وقت متبقٍ (`422` في الحالتين).
5. **Check-out**: مرفوض إن لم تكن الحالة `CHECKED_IN` أصلاً، أي **بدون تسجيل دخول سابق لا يوجد تسجيل خروج** (`409`).

**عند نجاح Check-in**: يبدأ **مؤقّت مهلة وصول 30 ثانية** (`bufferEndsAt`)، وبعدها يبدأ الاحتساب الفعلي
لمدة الحجز المدفوعة (`actualStartTime` → `expectedEndTime`)، وتُخزَّن الحقول الثلاثة في سجل
`CheckInLog` المرتبط.

**Response `200` (Check-in)**
```json
{
  "message": "تم تسجيل الحضور بنجاح — مهلة الوصول للمقعد 30 ثانية قبل بدء احتساب الوقت",
  "booking": {
    "status": "CHECKED_IN",
    "checkInLogs": [
      {
        "action": "CHECK_IN", "timestamp": "2026-07-22T10:00:05.000Z",
        "bufferEndsAt": "2026-07-22T10:00:35.000Z",
        "actualStartTime": "2026-07-22T10:00:35.000Z",
        "expectedEndTime": "2026-07-22T11:00:35.000Z"
      }
    ]
  }
}
```

### `POST /api/bookings/:id/check-in`
مسار التكامل الرسمي البديل (عقد التكامل §10) — يحدَّد الحجز عبر معرّفه في الـ URL مباشرة (لا
`bookingCode` ولا `qrCode`). **Auth**: يتطلب جلسة (`401` بدونها). يشترك مع `/api/checkin` في نفس محرك
القيود أعلاه بالكامل (`src/lib/checkin-core.ts` مشترك بين المسارين) — الفرق الوحيد أنه يدعم
`idempotencyKey` لمنع إعادة معالجة نفس الحدث عند إعادة الإرسال (Replay).

**Request body**
```json
{ "eventType": "CHECK_IN", "idempotencyKey": "client-generated-uuid" }
```
| الحقل | ملاحظات |
|---|---|
| `eventType` | `CHECK_IN` أو `CHECK_OUT` (يقبل أيضاً `action` كاسم مرادف) |
| `idempotencyKey` | اختياري — إن كان مطابقاً لمفتاح مُستخدَم سابقاً على أي حجز، يُرفض الطلب فوراً بـ`409` دون أي معالجة أو إنشاء سجل جديد |

الاستجابة بنفس شكل `/api/checkin` تماماً (`{ booking, message }`).

---

## 7. لوحة العمليات (Staff-only)

### `GET /api/dashboard/summary`
**Auth**: موظفون فقط. إحصائيات لحظية (الحاضرون الآن، القادمون خلال ساعة، المتأخرون، إلخ) — بدون معاملات.

---

## 8. جدول رموز الحالة الشامل

| الكود | المعنى |
|---|---|
| 200 | نجاح (قراءة/تحديث) |
| 201 | تم الإنشاء (حجز/حساب جديد) |
| 400 | بيانات غير صالحة أو ناقصة |
| 401 | يجب تسجيل الدخول |
| 403 | لا تملك الصلاحية الكافية |
| 404 | العنصر غير موجود |
| 409 | تعارض (حجز مزدوج، حالة غير متسقة) |
| 422 | مرفوض منطقياً (قاعدة عمل — مثال: خارج نافذة الوصول) |
| 429 | محاولة متكررة خلال فترة التهدئة |
| 500 | خطأ سيرفر غير متوقع |

---

## 9. ملاحظات أمنية للمكامِلين

- **لا تُرسل أبداً حقول سعر** في `POST /api/bookings` — أي حقل كهذا يُتجاهل تماماً لأن المخطط (Zod schema) لا يحتوي عليه أصلاً؛ السعر النهائي يأتي فقط في الاستجابة.
- **معرّفات الكيانات (IDs) كلها نصوص `cuid()`** — لا تفترض أنها أرقام أو أنها متسلسلة.
- كل المسارات التي تُغيّر بيانات (`POST`/`PATCH`) تتحقق من الصلاحيات على السيرفر بغض النظر عمّا تعرضه الواجهة — راجع `src/lib/session.ts` و`src/lib/rbac.ts`.

---

## 10. مطابقة معايير التكامل (`INTEGRATION_STANDARDS.pdf`)

هذا القسم يقارن نموذج بياناتنا الفعلي بالشكل الرسمي المطلوب في وثيقة معايير التكامل ومجموعة اختبارات
Postman الأمنية المرسلة من الإدارة (المرجع النهائي والملزم). **تحديث**: تم تنفيذ الترحيل الخاص بشكل
الحجز/الاستجابة/المصادقة بالكامل (انظر §5 و§5.ب و§5.ج أعلاه) — الفروقات المتبقية أدناه (أدوار
المستخدم، شكل الصلاحيات، أسماء حقول تسعير المساحة) لم تُنفَّذ بعد وتحتاج قراراً صريحاً لاحقاً.

### 10.1 ما يطابق المعايير فعلاً بدون أي تعديل ✅

| المعيار المطلوب | حالتنا |
|---|---|
| `String IDs (cuid/uuid)`, لا أرقام تسلسلية | ✅ نستخدم `cuid()` في كل النماذج |
| `ISO 8601` للتواريخ | ✅ كل حقول `DateTime` تُسلسَل بصيغة `2026-08-01T09:00:00.000Z` |
| `REST over JSON`, لا GraphQL/RPC | ✅ Route Handlers قياسية (`GET/POST/PATCH`) |
| صيغة الخطأ `{ "error": "..." }` + أكواد `400/401/403/404/409/429` | ✅ مطابقة تماماً (انظر §1 أعلاه) |
| الأسعار تُحسب سيرفرياً دائماً، لا تُقبل من العميل | ✅ `calculatePrice()` — لا حقل سعر في أي مخطط إدخال |
| الهوية من الجلسة الموثَّقة لا من الـ URL/query | ✅ `session.user.id` من NextAuth JWT فقط |
| PostgreSQL + Prisma ORM | ✅ |
| Next.js 14 App Router + TypeScript | ✅ |
| واجهة عربية RTL | ✅ |
| Zod للتحقق من كل مدخل | ✅ |
| حجوزات الضيوف (`userId` قابل للـ null + بيانات ضيف) | ✅ `guestName/guestPhone/guestEmail` |
| `startDate`/`endDate` في طلب/استجابة الحجز | ✅ مضافان كمرادفين لـ `startTime`/`endTime` |
| `bookingType` غير حساس لحالة الأحرف | ✅ `"hourly"` يُطبَّع تلقائياً لـ `"HOURLY"` |
| استجابة الحجز مسطّحة (بدون تغليف `{booking:{...}}`) | ✅ `POST/GET/PATCH/DELETE` على `/api/bookings*` كلها |
| حجز جديد يبدأ `PENDING` لا `CONFIRMED` تلقائياً | ✅ يحتاج تأكيداً صريحاً عبر `PATCH /api/admin/bookings/:id` |
| `401` (لا جلسة) منفصل عن `403` (جلسة صالحة، صلاحية ناقصة) | ✅ `requireStaffSession`/`assertPermission` لا يخلطان بينهما |
| `DELETE /api/bookings/:id` للإلغاء الذاتي | ✅ + `PATCH`/`DELETE /api/admin/bookings/:id` للإدارة |
| `POST /api/bookings/:id/check-in` + `idempotencyKey` | ✅ يشارك محرك القيود مع `/api/checkin` |
| `GET /api/availability` | ✅ فتحات بالساعة حسب `weeklyAvailability` |
| حجز بلا جلسة إطلاقاً (ضيف مجهول) | ✅ أُلغِي تماماً — `401` دائماً بلا جلسة، حتى لو أُرسِلت بيانات ضيف |

### 10.2 الفروقات المتبقية — تحتاج قراراً وترحيلاً لاحقاً ⚠️

| الجانب | معياري (`INTEGRATION_STANDARDS.pdf`) | حالتنا الآن | الأثر |
|---|---|---|---|
| **أدوار المستخدم** | `role: "USER" \| "ADMIN"` فقط، مع `isSuperAdmin: boolean` منفصل للتمييز داخل الإدارة | 4 قيم Enum: `USER / RECEPTION / ADMIN / SUPER_ADMIN` | نموذجنا أكثر تفصيلاً (يفرّق استقبال عن مدير) وهو **متوافق مع الـ PRD الأصلي** (الذي يطلب صراحة أدوار استقبال/مشرف/مدير منفصلة، §5 "07-C-FLOW"). عند الدمج: يمكن تعيين `RECEPTION → ADMIN` مع `isSuperAdmin: false` وصلاحيات مقيّدة، و`SUPER_ADMIN → ADMIN` مع `isSuperAdmin: true`، دون فقد المعلومة فعلياً — يحتاج فقط عمود `isSuperAdmin` إضافي وقت الترحيل. |
| **شكل الصلاحيات** | مصفوفة نصوص مفاتيح `"[\"bookings\",\"spaces.view\"]"` بصيغة `section.action` | كائن Boolean لكل صلاحية (`{ canCheckIn: true, ... }`) | يحتاج دالة تحويل (Adapter) في اتجاهين: `permissionsObjectToKeys()` و`keysToPermissionsObject()`. منطق الفحص (`hasPermission`) في `src/lib/rbac.ts` يبقى كما هو داخلياً؛ فقط طبقة التسلسل (serialization) عند التصدير/الاستيراد تتغيّر. |
| **حقول تسعير المساحة** | `priceHourly / priceDaily / priceMonthly / price4Hours` (باقة شهرية واحدة) | `hourlyPrice / fourHourPrice / dailyPrice / monthlyMorningPrice / monthlyEveningPrice` (شهري صباحي/مسائي منفصلان) | تقسيم الشهري لصباحي/مسائي **مطلوب صراحة في الـ PRD الأصلي لرمال X** (وليس اختيارياً)؛ عند الدمج يُقترح تمثيله كـ Tier إضافي ضمن مصفوفة الباقات المتدرجة (`tier-1/tier-2`) التي تذكرها المعايير للتسعير المُجمَّع، بدل حقل ثابت. |
| **التوفر الأسبوعي للمساحة** | صف لكل يوم: `{ dayOfWeek: 0-6, startTime, endTime, isActive }` | كائن JSON واحد (`{ sun: {open,close}, mon: {...}, ... }`) | نفس المعلومة بشكل مختلف — تحويل مباشر وبسيط (loop على أيام الأسبوع). |
| **أسماء حقول تسعير الحجز** | `originalPrice / totalPrice` | `basePrice / finalPrice` | `discountAmount` متطابق فعلاً؛ البقية إعادة تسمية فقط (Alias) عند التصدير — لم تُنفَّذ بعد لأن اختبارات Postman لا تفرضها صراحة (تتحقق فقط من عدم الوثوق بسعر العميل، لا من اسم الحقل تحديداً). |
| **حالات الحجز (Status)** | مثال مصغّر: `PENDING → CONFIRMED / CANCELLED` فقط | 6 حالات كاملة: `PENDING/CONFIRMED/CHECKED_IN/CHECKED_OUT/CANCELLED/NO_SHOW` | الـ PRD الأصلي (§5 "03-C-FLOW") **يطلب صراحة** دورة كاملة تشمل Check-in/Check-out/No-show/Reschedule — مثال المعايير على الأرجح توضيحي مبسّط وليس سقفاً أعلى؛ نُبقي حالاتنا الغنية ونعامل `CHECKED_IN/CHECKED_OUT/NO_SHOW` كحالات تشغيلية إضافية تُصدَّر كحقل `operationalStatus` منفصل عن `status` الأساسي إن احتاج الدمج ذلك. |

### 10.3 خطة الترحيل المقترحة (عند الموافقة على البدء فيها)

1. إضافة عمود `isSuperAdmin Boolean` وحذف قيمتي `RECEPTION`/`SUPER_ADMIN` من enum الدور (تعيين `RECEPTION→ADMIN`, `SUPER_ADMIN→ADMIN+isSuperAdmin=true`) — **migration بيانات فعلية، تتطلب نسخة احتياطية أولاً**.
2. كتابة `permissionsObjectToKeys()`/`keysToPermissionsObject()` في `src/lib/rbac.ts` واستبدال حقل `permissions` بمصفوفة نصوص.
3. إضافة حقول Alias في استجابات `GET /api/bookings` و`GET /api/spaces` (`originalPrice`, `totalPrice`, `priceHourly`, إلخ) دون حذف الأسماء الحالية فوراً — فترة سماح (Deprecation window).
4. طبقة Adapter واحدة (`src/lib/integration-adapter.ts`) تعزل هذا التحويل بالكامل عن بقية الكود، تماماً كما يصف الـ PRD الأصلي (§3 "Adapter Integration واحد").

> **لن يبدأ أي بند من هذه الخطة تلقائياً** — ينفَّذ فقط بطلب صريح لاحق، لتفادي مخاطرة كسر النظام العامل قبيل النشر والاختبار.
